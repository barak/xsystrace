gtk frontend for systrace
-------------------------

To make the source, 

$ autoreconf --install
$ ./configure
$ make
$ make install

To use the gtk frontend with systrace, do

$ cd /usr/X11R6/bin && ln -s /usr/local/bin/notification xsystrace
